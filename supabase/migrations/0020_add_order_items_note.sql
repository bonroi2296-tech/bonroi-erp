-- 주문 내역 품목 줄에 사람이 직접 적는 메모 칸.
--
-- 왜 필요했나: "메디탑 알콜솜 144개"처럼 수량이 왜 그 숫자인지(= 3카톤 × 48개)
-- 나중에 보면 알 수 없었다. 거래처를 왜 바꿨는지 같은 것도 기록할 데가 없었다.
-- 주문 단위 메모(orders.note)는 소싱이 "소싱 자동 반영 · 강서" 로 채워 쓰고 있어
-- 사람이 쓸 자리가 아니었다.
--
-- nullable 이라 기존 1,747행과 이 칸을 모르는 배포판 코드에는 아무 영향이 없다.
alter table order_items add column if not exists note text;
