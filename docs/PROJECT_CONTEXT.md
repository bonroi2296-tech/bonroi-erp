# PROJECT_CONTEXT — 본로이 ERP 배경·현황·다음 할 일

> 결정의 "왜"와 현재 상태, PO가 정한 방향, 다음 작업을 담는다.
> 작업 규칙·빌드·컨벤션은 `CLAUDE.md` 참고.

## 0. 우리가 푸는 문제 (도메인)
본로이는 의료소모품 **유통사**다. 흐름은:
1. **병원(지점)이 주문**을 준다 (텍스트·이미지·엑셀 등 제각각).
2. 우리가 그걸 정리해 **여러 거래처에 나눠 발주**한다 (거래처마다 가격·배송비·재고가 다름).
3. 거래처가 **실제 출고 수량·단가·배송비**를 거래명세서로 알려준다.
4. 그걸 **최종 "종합 주문 내역"(매입 장부)에 쌓는다** → 마진·정산·세금계산서의 기준.

## 1. 기능 현황 (실데이터 기준)
| 화면(메뉴) | 경로 | 상태 | 비고 |
|---|---|---|---|
| 대시보드 | /dashboard | 사용 | `get_dashboard_stats` RPC |
| 주문 관리 | /orders | **핵심 사용** | orders 220 / order_items 1,140건 |
| 주문 내역 | /order-history | **핵심 사용** | 읽기 전용 장부 + CSV 내보내기 (= "종합 주문 내역") |
| 품목 마스터 | /products | 사용 | products 236 |
| 단가 비교 | /price-compare | 사용 | vendor_products 706 |
| 벤더 관리 | /vendors | 사용(기본) | vendors 12 |
| 공급망 관리 | /supply-monitor | 사용 | vendor_supply_status 171 |
| 확보 관리(소싱) | /sourcing, /sourcing/[id], /sourcing/parse | **신규·성장 중** | jobs 2. AI 파싱→거래처 분류→출고확인서 반영→완료 시 주문 내역에 누적 |
| 발주 관리 | /purchase | **미사용(0건)** | 소싱이 대체 중. 코드 보존·메뉴 숨김 예정 |
| 반품 관리 | /returns | 미사용(0건) | 코드 보존·메뉴 숨김 예정 |
| 문서 생성 | /documents | 미완(0건) | 엑셀 거래명세서는 `/api/invoice`로 별도 존재 |
| 분석/리포트 | /analytics | 미완 | 코드 보존·메뉴 숨김 예정 |
| 설정 | /settings | 껍데기 | 화면만 |

API: `/api/parse-order`(Gemini 주문파싱), `/api/reconcile`(Gemini 출고확인서), `/api/invoice`(엑셀 거래명세서, **현재 로그인 필요**).

## 2. 핵심 데이터 흐름과 "왜 이렇게 됐나"
- **두 갈래가 겹쳐 있다**: ① 주문 관리(`orders`)와 ② 확보 관리(소싱, `sourcing_*`)가 둘 다 "병원 주문 → 거래처별 발주"를 한다. 소싱이 **나중에 추가된 똑똑한 버전**(AI 파싱·가격비교·출고확인서 반영)이고, 옛 발주 관리(`purchase_orders`)는 사실상 안 쓴다(0건).
- **끊겼던 마지막 고리를 이번에 연결**: 소싱은 끝나도 어디에도 안 쌓였다. 이제 **거래처 "완료"(또는 출고확인서 반영) → `orders`/`order_items` 한 건 생성 → 주문 내역에 누적**된다. 멱등성은 `sourcing_settlements.order_id`로 보장(완료 취소/재반영 시 그 주문만 교체).
- **무로그인(anon) 운영**: 빠른 사용을 위해 로그인 게이트를 끄고 anon 키로 쓴다. 그 대가로 모든 테이블이 공개 상태(보안 부채).

## 3. PO가 정한 방향 (결정 완료 — 미결 아님)
1. **로그인 도입**: 지금은 **보류**. 시스템이 안정화되면 도입한다 → §5 다음 과제로 기록. (`NEXT_PUBLIC_AUTH_ENABLED`로 켤 수 있게 이미 준비됨)
2. **안 쓰는 화면(발주 관리·반품 관리·문서 생성·분석/리포트)**: **삭제하지 않는다. 메뉴(사이드바)에서만 숨기고 코드는 보존**한다. 나중에 부활 가능하게.
3. **주문 관리 ↔ 소싱 일원화**: **소싱으로 통합**하는 방향. 단 **큰 변경**이므로 — 기존 주문 220건 **보존**, 합치는 계획을 **먼저 쉬운 말로 보여주고 PO 승인받은 뒤** 진행. **한 번에 갈아엎지 않는다(점진적).**
4. **`/api/invoice` 로그인 의존 제거**: 무로그인 환경에서도 엑셀 거래명세서가 되도록 **로그인 요구를 푼다.**

## 4. 알려진 위험 / 주의
- **🔴 보안**: 전 테이블 anon 전체 공개. 공개 키만 알면 데이터 접근 가능. 로그인 도입 시(결정#1) 일괄 정리 필요.
- **🟠 마이그레이션 드리프트**: 라이브 DB에 repo에 없는 `temp_anon_*` 마이그레이션 존재. repo 0001~0005 주석("anon 차단")과 라이브 실제(개방)가 다르다. 진실은 라이브 DB.
- **🟠 에러 삼킴**: `console.error`로만 처리하는 곳 8군데 → 사용자에게 실패가 안 보임. 점진적으로 toast로 전환.
- **🟡 타입 우회**: `as unknown as` 42곳.
- **🟡 하드코딩**: `/api/invoice`에 공급자(본로이)·지점 정보 하드코딩 → 추후 DB로.
- **🟡 `GEMINI_API_KEY`가 `.env.example`에 없음** → 추가 필요.
- 성능(인덱스): FK 미인덱스·미사용 인덱스 다수지만 데이터가 작아 **우선순위 낮음**.

## 5. 다음 할 일 (우선순위 순)
1. **[승인 대기] 일원화 2단계**: `docs/UNIFICATION_PHASE2_PLAN.md` 의 계획대로 진행. PO 승인 후 시작.
2. **[승인 대기] 공공 API 연동**: 공공데이터포털 API 키 + `/api/catalog/lookup` 추가. 자세한 출처/엔드포인트는 `docs/CATALOG_SOURCES.md`. EDI 코드로 식약처 UDI/EDI 통합조회 → 제품 빈 칸만 자동 보완.
3. **[중간] 사진 업로드 UI**: 지금은 URL 붙여넣기만. Supabase Storage 버킷 + 파일 업로드 UI 추가.
4. **[중간] 코드 품질**: 조용한 `console.error`를 toast로, `.env.example`에 `GEMINI_API_KEY` 추가.
5. **[나중] 로그인 도입** (결정#1): 안정화 후. anon 정책 정리 동반. 이때 `/api/invoice` 게이트도 복원.
6. **[낮음] 성능 인덱스 정리**: FK 인덱스 추가/미사용 인덱스 제거.
7. **[검토] 거래처 사이트 스크래퍼**: 익명 GET 403(주사기닷컴 기준). 비용·리스크 큼. C(공공 API)와 A(명세서 누적)로 부족할 때만.

## 6. 최근 작업 이력 (이번 세션)
- #21: 주문 내역 납품가/마진 채움(소싱 자동) + `/api/invoice` 무로그인 + 미사용 메뉴 4개 숨김 (결정#2·#4 완료).
- #22: 소싱 품목별 납품가 손수정 (`demand_lines.supply_price`). 일원화 1단계.
- #23 #25: 주문 내역 가독성(촘촘+거래처 색칩 11종 고정).
- #24: 좌측 사이드바 → 상단 가로 바.
- #26: 소싱 하단 한 표로 통폐합 + 병원용 변경 내역 복사.
- #27 #28 #29: 제품(품목 마스터) 카탈로그 베이스 — 사진/박스수량/단위/설명 칸 추가, 표 → 썸네일 카드 그리드, 카드 가격(변동 가능) 표기·양방/한방 배지 제거.
- #30: 이름 "품목 마스터" → "제품". 출고확인서 AI가 EDI/박스/단위까지 추출 → 제품 빈 칸 자동 보완.
- (이번 PR): 색상 헬퍼 `src/lib/colors.ts` 공용화, 주문 관리·확보 관리 목록에 거래처/지점 색칩 적용. `docs/CATALOG_SOURCES.md` (식약처·HIRA API 조사) + `docs/UNIFICATION_PHASE2_PLAN.md` 추가.
- (#16~#19): 초기 사고/연결 작업 — anon 권한 복구, 드래그앤드롭+PDF, 소싱→주문 내역 연결, 주문번호 RPC.
- #17: 출고확인서 모달 드래그앤드롭 + PDF 첨부 지원. `sourcing_settlements` anon 권한 복구(마감 오류 해결).
- #18: **소싱 완료 → 주문 내역 연결**(핵심). "마감" 체크박스를 "완료" 버튼으로 교체. `sourcing_settlements.order_id` 추가.
- #19: `next_order_number()` RPC를 `security definer` + anon 실행 허용, 시퀀스 정렬(완료 반영 시 주문번호 오류 해결).
